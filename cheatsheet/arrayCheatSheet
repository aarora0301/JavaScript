Operations in Array :


Insert single element in array: (insertion happens at the last of array with push)
var arr = [1,2,3];
arr.push(4);
// arr is now [1,2,3,4]


Insert multiple element in array:
var ourArray = ["Stimpson", "J", "cat"];
ourArray.push(["happy", "joy"]); 
// ourArray now equals ["Stimpson", "J", "cat", ["happy", "joy"]]


Insert elements in array: (insertion happens at the start of array with unshift)
var ourArray = ["Stimpson", "J", "cat"];
ourArray.shift(); // ourArray now equals ["J", "cat"]
ourArray.unshift("Happy"); 
// ourArray now equals ["Happy", "J", "cat"]

var myArray = [["John", 23], ["dog", 3]];
myArray.shift();
myArray.unshift(["Paul",35]);
myArray should now have [["Paul", 35], ["dog", 3]].


Remove element from array : (deletion happens from he last of array with pop)
var threeArr = [1, 4, 6];
var oneDown = threeArr.pop();
console.log(oneDown); // Returns 6
console.log(threeArr); // Returns [1, 4]

var myArray = [["John", 23], ["cat", 2]];
var removedFromMyArray=myArray.pop();
console.log(removedFromMyArray);// Returns ["cat", 2]
console.log(myArray)//Returns [ [ 'John', 23 ] ]


Remove element from array: (deletion happens from the start of array with shift)
var ourArray = ["Stimpson", "J", ["cat"]];
var removedFromOurArray = ourArray.shift();
// removedFromOurArray now equals "Stimpson" and ourArray now equals ["J", ["cat"]].

var myArray = [["John", 23], ["dog", 3]];
var removedFromMyArray=myArray.shift();
//myArray should now equal [["dog", 3]], removedFromMyArray should contain ["John", 23].

